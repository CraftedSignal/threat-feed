---
title: Suspicious JavaScript File Upload to AWS S3 Static Website
slug: 2024-01-02-aws-s3-static-site-js-upload
description: Detection of a JavaScript file upload to an AWS S3 static website directory by an IAM user or assumed role, potentially indicating malicious web content modification and frontend compromise.
date: "2024-01-02T12:00:00Z"
type: advisory
types:
  - advisory
severities:
  - medium
tags:
  - aws
  - s3
  - static-website
  - javascript
  - web-defacement
vendors:
  - Amazon
products:
  - AWS S3
  - AWS IAM
  - AWS CloudTrail
  - AWS CloudFront
mitre_ttps:
  - tactic_id: TA0040
    tactic_name: Impact
    technique_id: T1491
    technique_name: Defacement
  - tactic_id: TA0040
    tactic_name: Impact
    technique_id: T1565
    technique_name: Data Manipulation
references:
  - https://www.sygnia.co/blog/sygnia-investigation-bybit-hack/
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/WebsiteHosting.html
  - https://docs.aws.amazon.com/AmazonS3/latest/API/API_PutObject.html
rules:
  - title: AWS S3 Static Site JavaScript File Upload
    description: Detects when a JavaScript file is uploaded to an S3 bucket directory commonly used for static website assets.
    platform: sigma
    severity: medium
    tactics:
      - impact
    techniques:
      - T1491.002
    data_sources:
      - cloudtrail
      - aws
      - s3
  - title: AWS S3 Bucket Accessed by Newly Created IAM User
    description: Detects when an S3 bucket is accessed by a recently created IAM user, which could indicate account compromise.
    platform: sigma
    severity: low
    tactics:
      - initial_access
    techniques:
      - T1078
    data_sources:
      - cloudtrail
      - aws
      - iam
rules_count: 2
---

This threat brief focuses on the detection of unauthorized modifications to static websites hosted on Amazon S3. Specifically, it addresses the risk of malicious actors uploading JavaScript files to directories commonly used for static site assets (e.g., `static/js/`). This activity can be indicative of a compromised IAM user or role being leveraged to inject malicious scripts into the website's frontend, potentially leading to defacement, data theft, or redirection of users to phishing sites. The focus is on identifying deviations from expected behavior, such as uploads originating from outside of established CI/CD pipelines or performed by unauthorized IAM entities. The rule described was last updated on 2026-04-10, and targets AWS CloudTrail logs. The detection is designed to exclude common automation tools like Terraform, Ansible, and Pulumi to reduce false positives.

## Attack Chain

1. An attacker gains unauthorized access to an AWS account, potentially through compromised IAM credentials or a misconfigured IAM role.
2. The attacker enumerates available S3 buckets to identify those hosting static websites, likely using `ListBuckets` or similar API calls.
3. The attacker identifies the target bucket and the directory containing JavaScript files (e.g., `static/js/`).
4. The attacker crafts a malicious JavaScript file designed to inject malicious code into the website.
5. The attacker uploads the malicious JavaScript file to the identified S3 bucket and directory using the `PutObject` API call.
6. If CloudFront is used, the attacker may invalidate the CloudFront cache to ensure the malicious JavaScript file is immediately served to users.
7. Website visitors unknowingly execute the malicious JavaScript code, leading to client-side attacks such as data theft or redirection to phishing sites.
8. The attacker achieves the objective of defacing the website, stealing user data, or spreading malware.

## Impact

A successful attack can lead to defacement of the website, reputational damage, and potential loss of user data. The compromise of the website's frontend can allow attackers to inject malicious scripts that steal user credentials, redirect users to phishing sites, or spread malware. The number of affected users depends on the website's traffic. This type of attack is particularly concerning for e-commerce websites, financial institutions, and other organizations that handle sensitive user data. Mitigation requires swift action to remove the malicious code and remediate the compromised credentials.

## Recommendation

*   Deploy the Sigma rule "AWS S3 Static Site JavaScript File Upload" to your SIEM and tune for your specific environment (see rule below).
*   Investigate any alerts generated by the Sigma rule, focusing on the `aws.cloudtrail.user_identity.arn`, access key ID, and session type.
*   Review the content of uploaded JavaScript files using S3 `GetObject` or CloudTrail `requestParameters` to identify potentially malicious code.
*   Audit IAM policies to ensure that only authorized users and roles have write access to S3 buckets hosting static websites.
*   Enable S3 bucket versioning to facilitate quick rollback and historical review of file modifications.
*   Monitor AWS CloudTrail logs for `PutObject` events targeting S3 buckets containing static website content.
