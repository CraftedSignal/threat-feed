---
title: Novu XSS Vulnerability via Incomplete Sanitization
slug: 2024-01-09-novu-xss
description: A cross-site scripting (XSS) vulnerability exists in Novu due to incomplete sanitization of HTML attributes in email previews, allowing arbitrary JavaScript execution.
date: "2024-01-09T14:30:00Z"
type: advisory
types:
  - advisory
severities:
  - high
tags:
  - xss
  - cross-site-scripting
  - sanitization
  - novu
vendors:
  - Novu
products:
  - Novu
mitre_ttps:
  - tactic_id: TA0001
    tactic_name: Initial Access
    technique_id: T1190
    technique_name: Exploit Public-Facing Application
references:
  - https://github.com/advisories/GHSA-26wg-9xf2-q495
rules:
  - title: Detect Suspicious oncontentvisibilityautostatechange Attribute
    description: Detects the use of the oncontentvisibilityautostatechange attribute in web requests, potentially indicating an XSS attempt.
    platform: sigma
    severity: high
    tactics:
      - initial_access
    techniques:
      - T1190
    data_sources:
      - webserver
      - linux
  - title: Detect Suspicious HTML Attributes in Email Body
    description: Detects suspicious HTML attributes in email bodies, indicative of potential XSS attempts.
    platform: sigma
    severity: medium
    tactics:
      - initial_access
    techniques:
      - T1190
    data_sources:
      - webserver
      - linux
rules_count: 2
---

A cross-site scripting (XSS) vulnerability has been identified in Novu versions prior to 3.15.0. The vulnerability stems from incomplete sanitization of HTML attributes within the email preview functionality. Specifically, the application fails to properly sanitize the `oncontentvisibilityautostatechange=` attribute, allowing an attacker to inject arbitrary JavaScript code. This injected code executes when a user views an email containing the malicious HTML, potentially leading to account takeover or other malicious activities. This is more impactful than a self-XSS because an attacker can craft a URL that, when visited, logs the victim into the attacker's account and triggers the XSS.

## Attack Chain

1.  An attacker crafts a malicious HTML payload containing the `oncontentvisibilityautostatechange=` attribute with embedded JavaScript.
2.  The attacker injects this payload into the body of an email template within the Novu workflow editor.
3.  A user views the email template preview or receives an email generated from the compromised template.
4.  The user's browser parses the HTML, including the unsanitized `oncontentvisibilityautostatechange=` attribute.
5.  The JavaScript code embedded within the attribute executes in the user's browser, within the context of the Novu application.
6.  The attacker can leverage the XSS to steal session cookies or inject malicious code into the Novu application.
7.  The attacker can use the Google/GitHub OAuth flows without completing the code callback step, and send that URL to the victim.
8.  By logging into the attacker's account, the prepared XSS payload is triggered.

## Impact

Successful exploitation of this XSS vulnerability could allow an attacker to steal sensitive user data, including session cookies and API keys. An attacker can also perform actions on behalf of the victim, such as modifying workflow templates or sending malicious notifications. In scenarios where multiple users have access to the Novu dashboard, the impact can be amplified, leading to widespread compromise of user accounts and sensitive data. The vulnerability affects Novu versions prior to 3.15.0.

## Recommendation

*   Upgrade Novu to version 3.15.0 or later to patch the XSS vulnerability.
*   Deploy the Sigma rule `Detect Suspicious oncontentvisibilityautostatechange Attribute` to your SIEM to detect attempts to exploit this vulnerability via crafted email templates.
*   Regularly audit and review the sanitization mechanisms within your Novu application to ensure they are effective against new and emerging XSS attack vectors.
