---
title: Orbit Agent Local Privilege Escalation via Tcl Command Injection
slug: 2024-01-26-orbit-lpe
description: The Orbit agent is vulnerable to local privilege escalation due to Tcl command injection, where a crafted password containing '}' can inject arbitrary Tcl commands and allow an unprivileged local user to execute commands as root.
date: "2024-01-26T12:00:00Z"
type: advisory
types:
  - advisory
severities:
  - critical
tags:
  - privilege-escalation
  - tcl-injection
  - macos
vendors:
  - Fleet
products:
  - Orbit agent
mitre_ttps:
  - tactic_id: TA0004
    tactic_name: Privilege Escalation
    technique_id: T1059
    technique_name: Command and Scripting Interpreter
references:
  - https://github.com/advisories/GHSA-rphv-h674-5hp2
rules:
  - title: Detect Tcl Execution via Expect with Command Injection
    description: Detects the execution of Tcl scripts via 'expect' with command injection attempts based on argument patterns.
    platform: sigma
    severity: critical
    tactics:
      - privilege_escalation
    techniques:
      - T1059.004
    data_sources:
      - process_creation
      - macos
  - title: Detect Expect Process with Suspicious Arguments
    description: Detects 'expect' process executions with arguments indicative of command injection, focusing on potentially malicious characters or sequences.
    platform: sigma
    severity: high
    tactics:
      - privilege_escalation
    techniques:
      - T1059.004
    data_sources:
      - process_creation
      - macos
rules_count: 2
---

The Orbit agent, specifically versions prior to 4.81.1 of `go/github.com/fleetdm/fleet/v4`, contains a local privilege escalation vulnerability related to FileVault disk encryption key rotation on macOS. The agent collects a local user's password via a GUI dialog and directly interpolates it into a Tcl/expect script executed as root. A flaw exists in how the password is handled within the Tcl script. If a password contains a '}' character, it terminates the intended literal string and allows the injection of arbitrary Tcl commands. This allows a local unprivileged user to escalate their privileges to root. This vulnerability, identified as CVE-2026-27806, poses a significant risk to organizations using affected versions of the Orbit agent as any local user can gain full control of the system.

## Attack Chain

1.  User initiates FileVault disk encryption key rotation via Orbit agent.
2.  Orbit agent prompts the user for their password through a GUI dialog.
3.  User enters a password containing the '}' character, crafting a Tcl injection payload.
4.  Orbit agent constructs a Tcl/expect script, interpolating the malicious password string into the script.
5.  The agent executes the script as root using `exec.Command("expect", "-c", script)`.
6.  The '}' character in the password terminates the literal string within the Tcl script, injecting arbitrary Tcl commands.
7.  The injected Tcl commands are executed with root privileges.
8.  The attacker gains root access and can perform any action on the system.

## Impact

Successful exploitation of this vulnerability allows any unprivileged local user to gain root access on a macOS system running a vulnerable version of the Orbit agent. This could lead to complete system compromise, including data theft, modification, or destruction, and installation of malware. The vulnerability impacts all organizations using the affected Orbit agent versions, and could affect hundreds or thousands of endpoints if widely deployed.

## Recommendation

*   Upgrade the Orbit agent to version 4.81.1 or later to patch CVE-2026-27806.
*   Deploy the Sigma rule "Detect Tcl Execution via Expect with Command Injection" to identify potential exploitation attempts in your environment.
*   Monitor process creation events for the execution of `expect` with unusual command-line arguments, indicative of Tcl injection, using the Sigma rule "Detect Expect Process with Suspicious Arguments".
*   Implement restrictions on local user access to prevent unauthorized access and potential exploitation of the vulnerability.
