---
title: Detection of Unauthorized Access to macOS Web Browser Sensitive Files
slug: 2026-08-macos-browser-access
description: This detection identifies potential credential theft by monitoring untrusted processes or osascript attempting to access sensitive browser session cookies and password databases on macOS.
date: "2026-08-28T21:07:12Z"
type: advisory
types:
  - advisory
severities:
  - high
tags:
  - credential-access
  - macos
  - infostealer
affected_os:
  - macOS
mitre_ttps:
  - tactic_id: TA0006
    tactic_name: Credential Access
    technique_id: T1555
    technique_name: Credentials from Password Stores
    evidence: Adversaries may acquire credentials from web browsers by reading files specific to the target browser.
    confidence_band: high
  - tactic_id: TA0009
    tactic_name: Collection
    technique_id: T1005
    technique_name: Data from Local System
    evidence: The detection rule identifies such unauthorized access on macOS by monitoring file access events.
    confidence_band: high
references:
  - https://github.com/elastic/detection-rules/blob/main/rules/macos/credential_access_suspicious_web_browser_sensitive_file_access.toml
  - https://securelist.com/calisto-trojan-for-macos/86543/
rules:
  - title: Suspicious Web Browser Sensitive File Access
    description: Detects access to sensitive web browser files (cookies, login data) by unsigned/untrusted processes or osascript.
    platform: sigma
    severity: high
    tactics:
      - credential_access
    techniques:
      - T1555.003
    data_sources:
      - file_event
      - macos
rules_count: 1
action_plan:
  priority: elevated
  owners:
    - SOC
    - Detection Engineering
  immediate_actions:
    - action: Deploy the Sigma-compatible rule to the EDR platform.
      owner: Detection Engineering
      due: 48h
      evidence: Source provides high-fidelity detection logic for common credential access TTPs.
  hunt_leads:
    - lead: Search logs for unsigned processes interacting with browser directory paths.
      technique_id: T1555.003
      data_needed:
        - file_event logs
      priority: high
      confidence: high
      disposition: hunt_now
      evidence: Source emphasizes the use of unsigned/untrusted processes for credential theft.
  mitigation_plan:
    - priority: medium_term
      action: Enforce code signing requirements for all custom internal applications.
      owner: IT Operations
      addresses: Credential Access techniques
      evidence: Unsigned processes are the primary target of this detection.
---

Adversaries targeting macOS systems frequently attempt to exfiltrate stored credentials and session tokens to maintain persistence or conduct further unauthorized activities. By accessing files such as `cookies.sqlite`, `logins.json`, and various key databases located within browser-specific directories, attackers can bypass multi-factor authentication or directly access protected online services.

This threat brief focuses on detecting unauthorized processes - specifically those that are unsigned, untrusted, or leveraging the `osascript` utility - that attempt to interact with these sensitive files. Such behavior is a common indicator of modular malware or credential-stealing scripts. Defenders should monitor for these file-access patterns to identify potential information-stealing activity, as these files are rarely accessed by legitimate, non-browser-related background processes.

## Attack Chain

1. An attacker gains initial execution on a macOS system via phishing or an exploited application.
2. The attacker identifies the presence of target web browsers (e.g., Chrome, Safari, Brave) by inspecting the file system.
3. The attacker locates sensitive browser storage files, such as `Login Data` or `Cookies.binarycookies`, in the user's Library directory.
4. The attacker launches an unsigned binary or a malicious `osascript` payload to interact with these files.
5. The malicious process opens the sensitive files to read the contents into memory.
6. The exfiltrated data (cookies or passwords) is bundled and encrypted by the attacker's script.
7. The stolen data is exfiltrated to an attacker-controlled command-and-control server.

## Impact

Successful access to browser storage files results in the compromise of stored user credentials and active session tokens. This can lead to account takeover, unauthorized access to sensitive corporate or personal SaaS applications, and potential lateral movement within the network. In targeted attacks, this enables persistent access that persists even after browser restarts.

## Recommendation

1. Enable file-monitoring telemetry via Elastic Defend or equivalent EDR on all macOS endpoints.
2. Deploy the provided detection rule to identify unsigned or suspicious processes accessing browser-specific files.
3. Investigate any alerts generated by the rule to determine if the process is a legitimate backup tool, security agent, or a malicious credential stealer.
4. If malicious activity is confirmed, isolate the host and initiate a credential rotation for accounts associated with the compromised browser profiles.
